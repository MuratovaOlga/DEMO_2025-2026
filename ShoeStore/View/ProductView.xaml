<UserControl x:Class="ShoeStore.View.ProductView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ShoeStore.View"
             mc:Ignorable="d" 
             d:DesignHeight="250" d:DesignWidth="1000">
    <Grid Name="Back" Width="1000">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1.9*"/>
                <ColumnDefinition Width="0.6*"/>
            </Grid.ColumnDefinitions>

        <Border Grid.Column ="0" BorderBrush="Black" BorderThickness="2" Margin="5" Background="White">
            <Image Source="{Binding PhotoProduct}" Height="200" Width="200" Grid.Column="0" Margin="7 5 5 5" />
            </Border>

        <Border Grid.Column ="1" BorderBrush="Black" BorderThickness="1" Margin="5" Background="White">
                <StackPanel Grid.Column ="1" Orientation="Vertical" >
                    <StackPanel Orientation="Horizontal" Margin="5 2 0 5">
                    <TextBlock FontFamily="Times New Roman" FontSize="18" FontWeight="DemiBold">
                        <Run Text="{Binding IdProductCategoryNavigation.CategoryName}"/>
                        <Run Text=" | "/>
                        <Run Text="{Binding NameProduct}"/>
                    </TextBlock>
                </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="5 2 0 5">
                        <TextBlock Text="Описание товара: " FontFamily="Times New Roman" FontSize="18"/>
                        <TextBlock Text="{Binding DescriptionProduct}" Grid.Column="1" FontFamily="Times New Roman" FontSize="18" 
                           TextWrapping="WrapWithOverflow" Width="380"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="5 2 0 5">
                        <TextBlock Text="Производитель: " FontFamily="Times New Roman" FontSize="18"/>
                    <TextBlock Text="{Binding IdManufacturerNavigation.Name}" FontFamily="Times New Roman" FontSize="18"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="5 2 0 5">
                        <TextBlock Text="Поставщик: " FontFamily="Times New Roman" FontSize="18"/>
                    <TextBlock Text="{Binding IdSupplierNavigation.NameSupplier}" FontFamily="Times New Roman" FontSize="18"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="5 2 0 5">
                        <TextBlock Text="Цена: " FontFamily="Times New Roman" FontSize="18"/>
                        <TextBlock FontFamily="Times New Roman" FontSize="18">
                    <Run Name="Price" Text="{Binding PriceProduct, StringFormat = N2}"/>
                    <Run Name="PriceCurrent" Foreground="Black"/>
                        </TextBlock>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="5 2 0 5">
                        <TextBlock Text="Единица измерения: " FontFamily="Times New Roman" FontSize="18"/>
                        <TextBlock Text="{Binding UnitOfMeasurementProduct}" FontFamily="Times New Roman" FontSize="18"/>
                    </StackPanel>
                <TextBlock Name="QuantityInStock" FontFamily="Times New Roman" FontSize="18" Margin="5 2 0 5">
                    <Run Text="Количество на складе: "/>
                    <Run Text="{Binding QuantityInStockProduct}"/>
                </TextBlock>
                </StackPanel>
            </Border>
        <Border Grid.Column ="3" BorderBrush="Black" BorderThickness="2" Background="White" Margin="5">
            <TextBlock TextAlignment="Center" VerticalAlignment="Center" >
                    <Run Text="Действующая скидка: "/>
                    <Run Text="{Binding CurrentDiscountProduct}"/>
            </TextBlock>
            </Border>
        </Grid>
</UserControl>
